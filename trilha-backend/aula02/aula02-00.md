# AULA 02 - Básico de Javascript
## Conhecimentos básicos
- **Escopo:** é resumidamente tudo aquilo que está entre chaves ({});
- **Ponto e vírgula:** mesmo que já não precise, por boas práticas, deve-se colocar no final de toda linha de código ponto e vírgula;
- **Comentários:** são parte do código que estão ali para passar uma informação e não são executados. Os comentário podem ser:
    - **De linha única:** `//comentário`;
    - **De múltiplas linhas:** `/* comentário */`.

## Declaração de variáveis
Em Javascript, temos três métodos de declarar variáveis:
### var
O método mais simples e o menos recomendável! A variável `var` é declarada como:
```javascript
var nome = "algo";
```
ou
```javascript
var nome;
nome = "algo";
```
E o que ela implica para não ser recomendada? Há três propriedades dela:
#### Escopo
O `var` consegue sair e entrar de um escopo sem problema, podendo dar erro de sobrescrever uma varíavel de mesmo nome, mas em escopos diferentes.

Exemplo 1:
```javascript
var nome = "Aluno";
{ // isso abre outro escopo
    // console.log() é usado apra imprimir no terminal
    console.log(nome); // aparece no terminal "Aluno";
}
```
Exemplo 2:
```javascript
{
    var teste = 0;
}
console.log(teste); // irá printar o 0;
```

Mais tarde veremos sobre funções e você perceberá que vale apenas repetir alguns nomes de variáveis em outras funções para manter a legibilidade de seu código.

##### Redeclaração
O que é isso??? Bem, toda vez que você escreve `var nome = "aluno";` ou `var nome;`, está declarando uma variável. Redeclarar ela seria escrever a mesma variável em outra parte do código, sobrescrevendo-a, podendo ser um grande problema!

Exemplo 1:
```javascript
var nome = "Luiza";
console.log(nome);
var nome = "Antônio Carlos"; // a variável nome é redeclarada aqui
console.log(nome);
```

Exemplo 2:
```javascript
/* Veremos para que ser function mais para frente, mas mantenha em mente que a 
variável configuração na parte1 tem o valor "claro" e, o que se espera, é que ele
continue tendo esse valor. Mas ele é sobrescrito na parte2 para escuro, podendo
causar um erro muito grande! */

function parte1() {
    var configuracao = "claro";
}

function parte2() {
    var configuracao = "escuro"; // o primeiro é sobrescrito pelo segundo
}
```

#### Hoisting
O hoisting significa que, mesmo declarada depois, o programa não vai dar erro. Como assim? Olhe o exemplo abaixo:
```javascript
console.log(nome); // não vai dar erro, mas vai mostrar undifined (explicaremos mais para frente)
var nome = "Você";
```
Isso pode causar dor de cabeça quando algo está dando errado, mas não sabe o que é.

### Let
O `let` seria o var, mas sem as propriedades acimas. Ou seja, erro são mais fáceis de serem percebidos usando o `let`, além de ser um método mais seguro do que o `var`.
#### Escopo
Se você declarou um `let` em um escopo, ele fica praticamente invísivel em outro! Com os exemplos fica mais fácil de entender:

Exemplo 1:
```javascript
let cor = "azul";
{
    let cor = "vermelho";
    console.log(cor); // "vermelho"
}
console.log(cor); // "azul"
```

Exemplo 2:
```javascript
let nome = "Andre";
{
    console.log(nome);
    // Dá erro por conta de neste escopo o 'nome' não estar declarado
}
```

Exemplo 3:
```javascript
{
    let temporario = "dados";
    console.log(temporario); // "dados"
}
console.log(temporario); // ERRO: temporario não está definido aqui
```

#### Redeclaração
Quando se declara uma variável com `let`, não se pode declarar outra com o mesmo nome. Isso evita sobrescrição.

Exemplo 1:
```javascript
let idade = 19;
let idade = 21; // ISSO RESULTA EM UM ERRO!
```

#### Hoisting
Diferente do `var`, o `let` é obrigado a ser declarado antes de ser usado em qualquer situação.

Exemplo 1:
```javascript
console.log(cidade); // isso convoca um erro, pois o nome ainda não foi declarado
let cidade = "São Paulo";
```

### Const
O `const` não é uma variável, é uma constante (por isso o nome).
Aqui, o valor é fixo, não podendo trocar ele durante o código, ou seja:
```javascript
const NOME = "Um Nome aí";
NOME = "Outro"; // Evoca um erro, pois não pode ser mudado o valor da constante
```
Isso acaba por obrigar o programador à ter que já dar um valor para ele.

A constante acaba por ajudar em caso um valor precise ser fixo, além de outros casos ter uma maior eficiência de execução.

Suas propriedades de escopo, redeclaração e hoisting são as mesmas que o let, por isso será somente passado exemplos deles daqui à diante 😉.

(**BOAS PRÁTICAS:** as constantes são sempre declaradas em caiax alta)

#### Escopo
Exemplo 1:
```javascript
const PI = 3.14;
{
    const PI = 3.1415; 
    // Por estar em outro escopo, é possível declarar com o mesmo nome
    console.log(PI); // resultado: 3.1415
}
console.log(PI); // resultado: 3.14
```

Exemplo 2:
```javascript
const NOME = "Andre";
{
    console.log(NOME);
    // Aqui muda, pois não dá erro e mostra o "Andre"
}
```

Exemplo 3:
```javascript
const NOME = "Andre";
{
    console.log(NOME);
    // Agora aqui dá erro, por conta da constante ser declarada em baixa, logo escopo considera a que está declarada logo abaixo.
    const NOME = "Umbra";
}
```
#### Redeclaração
Uma constante não pode ser redeclarada em um mesmo escopo, mas pode ser declarada em escopos diferentes.
Exemplo 1:
```javascript
const DESCRICAO = "Morango";
{
    const DESCRICAO = "Leite"; 
    // Por estar em outro escopo, é possível declarar com o mesmo nome
    console.log(DESCRICAO); // resultado: "Leite"
}
console.log(DESCRICAO); // resultado: "Morango"
```

#### Hoisiting
A constante deve ser declarada antes de ser usada em qualquer outra parte do código!

Exemplo 1:
```javascript
const EMPRESA = "Sa";
{
    console.log(EMPRESA);
    // Agora aqui dá erro, por conta da constante ser declarada em baixa, logo escopo considera a que está declarada logo abaixo.
    const EMPRESA = ".S.A.";
}
```
