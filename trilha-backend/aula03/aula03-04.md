### Post
Aqui que vamos começar a modificar os dados da nossa base de dado. Você se lembra do body? Vamos o usar bastante aqui por diante!

Pense na situação: vou adicionar mais item à minha cesta do mercado, como posso fazer isso? Lógico, somos maníaco por organização, deixo minhas frutas separadas das outras coisas, assim como a mistura e o básico.

Bem, o corpo pode ser qualquer coisa! Aqui vamos usar o formato JSON para receber as informações, pois é um padrão no mercado!

Duranta uma requisição POST, recebemos o seguinte JSON:
```json
{
    "campo":"mistura",
    "novos": ["Acém", "Fígado"]
}
```

A partir daí, começamos o POST:
```javascript
import express from "express";

// Lista de um mercado
const mercado = {
    frutas: ["Banana", "Maçã", "Abacaxi"],
    mistura: ["Ovo", "Carne moída"],
    basicos: ["Arroz", "Feijão"]
}

const app = express();
app.use(express.json()); // Aqui vai permitir enviar um objeto JSON pelo body e como resposta ({objeto: "isto é um objeto"})

//GET's
//...

app.post("/colocar-item", (req, res) => {
    // Aqui pegamos todos os elementos do body
    const body = req.body;
    // O try vai tentar algo, se der erro, ele vai para o
    // catch e resolve lá
    try {
        const campo = body.campo;
        const values = body.novos;
        mercado[campo].push(...values);

        return res.status(200).json({response: "Adicionado com sucesso!"})
    } catch (err) {
        return res.status(500).json({error: err});
    }
});

app.listen(8000);
```
Indo para o Insominia, vamos cria mais uma requisição para o POST.
![Tela 9](./imgs/insomnia-requisicao-post.png)
Agora é renomear para um nome melhor e depois trocar o GET para o POST:
![Tela 10](./imgs/insomnia-requisicao-post-1.png)
![Tela 11](./imgs/insomnia-requisicao-post-2.png)

Agora não é só clicar em send, pois ainda não temos o body!

Indo na opção Body (Corpo, em português), não vamos ter nada, mas clicando em `No Body` podemos ver que há diversas tipos de Body, vamos ir em JSON.
![Tela 12](./imgs/insomnia-requisicao-post-3.png)
![Tela 13](./imgs/insomnia-requisicao-post-4.png)
![Tela 14](./imgs/insomnia-requisicao-post-5.png)

Agora adicionamos o JSON:
```json
{
    "campo":"mistura",
    "novos": ["Acém", "Fígado"]
}
```
![Tela 15](./imgs/insomnia-requisicao-post-6.png)

Daí agora só clicar em send e pronto!
![Tela 16](./imgs/insomnia-requisicao-post-7.png)

Podemos verificar novamente usando GET.
![Tela 17](./imgs/insomnia-requisicao-post-8.png)

### PATCH
Agora vamos usar o PATCH para atualizar nossa listinha. Aqui vamos usar o conceito dos parâmetros na url e do body. Vamos nessa!

Vamos agora criar uma nova requisição, renomeá-la e mudar de GET para PATCH.

Aqui, como **exemplo**, vamos usar uma a seguinte URL: `http://localhost:8000/atualizar-lista/:campo`, que o campo será `frutas`. Aqui, vamos receber do usuário o JSON: 
```json
{
    "item_antigo": "Banana",
    "item_novo": "Manga"
}
```

Bem, agora vamos para o código:
```js
import express from "express";

// Lista de um mercado
const mercado = {
    frutas: ["Banana", "Maçã", "Abacaxi"],
    mistura: ["Ovo", "Carne moída"],
    basicos: ["Arroz", "Feijão"]
}

const app = express();
app.use(express.json()); // Aqui vai permitir enviar um objeto JSON pelo body e como resposta ({objeto: "isto é um objeto"})

//GET's
//...

//POST
//...

app.patch("/atualizar-lista/:campo", (req, res) => {
    try{
        const campo = req.params.campo;
        const {item_antigo, item_novo} = req.body;
        const pos = mercado[campo].findIndex(item_antigo);

        if(!pos) return res.status(404).json({error: "Item não encontrado"});

        mercado[campo][pos] = item_novo;

        return res.status(200).json({message: "Atualizado!"});
    } catch (err) {
        
        return res.status(500).json({message: "Erro inesperado"});
    } 
});

app.listen(8000);
```

Enviando esse body e não dar nenhum erro, só conferir no GET.

**Criar a requisição**
![Tela 18](./imgs/insomnia-requisicao-patch-1.png)
**Mudar o método para PATCH**
![Tela 19](./imgs/insomnia-requisicao-patch-2.png)
![Tela 20](./imgs/insomnia-requisicao-patch-3.png)
**Escrever a URL e settar o falor do *campo***
![Tela 21](./imgs/insomnia-requisicao-patch-4.png)
**Ir para o body, clicar no *No Body* e escolher a opção JSON**
![Tela 22](./imgs/insomnia-requisicao-patch-5.png)

**Escrever o JSON e enviar**
![Tela 23](./imgs/insomnia-requisicao-patch-6.png)

**Ver o antes e o depois**
*Antes*
![Tela 24](./imgs/insomnia-requisicao-patch-0.png)
*Depois*
![Tela 25](./imgs/insomnia-requisicao-patch-7.png)

### DELETE
É um método mais simples que o PATCH, mas também o mais destruitivo e que se tem que tomar cuidado!
O DELETE indica que vai ter algum delete na sua base de dado, então é de extrema importânica saber se está deletando o dado certo e não a base toda!

Como exemplo, vamos deletar o último elemento de qualquer uma das listas! Primeiro, o usuário vai informar o campo que ele quer deletar: *frutas*, *misturas* ou *básicos*. Daí, vamos criar uma rota de solicite isso e, se não vier vazio, deletar o último item.
```js
import express from "express";

// Lista de um mercado
const mercado = {
    frutas: ["Banana", "Maçã", "Abacaxi"],
    mistura: ["Ovo", "Carne moída"],
    basicos: ["Arroz", "Feijão"]
}

const app = express();
app.use(express.json()); // Aqui vai permitir enviar um objeto JSON pelo body e como resposta ({objeto: "isto é um objeto"})

//GET's
//...

//POST
//...

//PATCH
//...

app.delete("/deletar-ultimo-item/:campo", (req, res) => {
    try {
        const campo = req.params.campo;
        mercado[campo].pop();
    } catch (err) {
        return res.status(500).json({error: "Erro ao deletar o último elemento!"});
    }
});

app.listen(8000);
```

**Criando uma nova requisição**
![Tela 26](./imgs/insomnia-requisicao-delete-1.png)
**Trocando-a para DELETE**
![Tela 27](./imgs/insomnia-requisicao-delete-2.png)
**Escrevendo a URL, preenchendo o *campo* e enviando**
![Tela 28](./imgs/insomnia-requisicao-delete-3.png)
**Verificando as mudanças**
![Tela 29](./imgs/insomnia-requisicao-delete-0.png)
![Tela 30](./imgs/insomnia-requisicao-delete-4.png)

### PUT
O PUT é um PATCH, mas só que o PATCH precisa que o elemento exista para poder atualizar, quanto o PUT, caso o caso não exista, ele cria um novo registro.

O processo de criar uma requisição PUT é idêntico ao do PATCH. Fica o desafio fazer uma respota PUT! 😉